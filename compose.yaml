services:
  game:
    build: .
    ports:
      - "8080:80"
    depends_on:
      - login
      - websocket

  login:
    build: ./login
    expose:
      - "4000"

  websocket:
    build: ./websocket
    ports:
      - "3000:3000"
    volumes:
      - ./database:/database